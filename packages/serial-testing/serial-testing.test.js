'use strict'
/* eslint-disable no-new */

const Buffer = require('safe-buffer').Buffer
const sinon = require('sinon')
const SerialTesting = require('./serial-testing')

describe('SerialTesting', () => {
  it('constructs', () => {
    new SerialTesting()
  })
  it('needs more testing')
})
